- cannot resume audio on iOS after screen lock (broken). it can be resolve by manually reloading the page. can this be automated or fixed otherwise? if the unlock can be detected, maybe a resume audio confirmation dialog could do the trick
- search after 1 input character should not wait for the searcxh.
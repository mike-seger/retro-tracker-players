{
  "name": "sid-compat-scan",
  "version": "1.0.0",
  "description": "Offline SID file compatibility scanner — produces MD5 + jsSID playability TSV for large collections",
  "main": "scan.js",
  "scripts": {
    "scan": "node scan.js",
    "scan:metadata-only": "node scan.js --workers 16 --batch 500"
  },
  "engines": {
    "node": ">=18"
  }
}
